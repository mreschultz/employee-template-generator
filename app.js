const Manager = require("./lib/Manager");

manager = new Manager(answers.name, answer.id, answer.email);

const manager = new Manager(answers.name, answer.id, answer.email);

teamZArray.push(manager);
